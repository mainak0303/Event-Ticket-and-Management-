<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Events - Organizer</title>

  <!-- Stylesheets -->
  <link href="/vendor/fontawesome-free/css/all.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css?family=Nunito:200,300,400,700,900" rel="stylesheet" />
  <link href="/css/sb-admin.css" rel="stylesheet" />
</head>

<body id="page-top">
  <div id="wrapper">

    <!-- Sidebar -->
    <%- include('partials/sidebar', { activePage: 'events' }) %>

    <div id="content-wrapper" class="d-flex flex-column">
      <div id="content">

        <!-- Navbar -->
        <%- include('partials/navbar', { event_organizer: organizer, success, error }) %>

        <div class="container-fluid">

          <h1 class="h3 mb-4 text-gray-800">Events</h1>

          <% if (success) { %>
            <div class="alert alert-success alert-dismissible"><%= success %></div>
          <% } %>
          <% if (error) { %>
            <div class="alert alert-danger alert-dismissible"><%= error %></div>
          <% } %>

          <div class="mb-3">
            <a href="/organizer/events/new" class="btn btn-primary">
              <i class="fas fa-plus"></i> Add New Event
            </a>
          </div>

          <form method="GET" action="/organizer/events" class="mb-4">
  <div class="form-row">
    <div class="col">
      <label for="filterDate">Date</label>
      <input type="date" name="date" id="filterDate" class="form-control" value="<%= filters.date || '' %>" />
    </div>
    <div class="col">
      <label for="filterTime">Time</label>
      <input type="time" name="time" id="filterTime" class="form-control" value="<%= filters.time || '' %>" />
    </div>
    <div class="col">
      <label for="filterLocation">Location</label>
      <input type="text" name="location" id="filterLocation" class="form-control" placeholder="Location" value="<%= filters.location || '' %>" />
    </div>
    <div class="col">
      <label for="filterCategory">Category</label>
      <select name="category" id="filterCategory" class="form-control">
        <option value="">All Categories</option>
        <% categories.forEach(cat => { %>
          <option value="<%= cat._id %>" <%= filters.category === cat._id.toString() ? 'selected' : '' %>><%= cat.name %></option>
        <% }) %>
      </select>
    </div>
    <div class="col">
      <label for="filterSubcategory">Subcategory</label>
      <input type="text" name="subcategory" id="filterSubcategory" class="form-control" placeholder="Subcategory" value="<%= filters.subcategory || '' %>" />
    </div>
    <div class="col d-flex align-items-end">
      <button type="submit" class="btn btn-primary mr-2">Filter</button>
      <a href="/organizer/events" class="btn btn-secondary">Reset</a>
    </div>
  </div>
</form>

          <table class="table table-bordered" id="eventsTable" width="100%" cellspacing="0">
            <thead class="thead-dark">
              <tr>
                <th>S.No</th>
                <th>Banner</th>
                <th>Title</th>
                <th>Date</th>
                <th>Time</th>
                <th>Location</th>
                <th>Category</th>
                <th>Subcategory</th>
                <th>Organizer</th>
                <th>Ticket Tiers</th>
                <th>Approval Status</th>
                <th>Status</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% let serial = 1; %>
              <% events.forEach(event => { %>
                <tr>
                  <td><%= serial++ %></td>
                  <td>
                    <% if (event.banner) { %>
                      <img src="/uploads/<%= event.banner %>" alt="Banner" style="width: 70px; height: 50px; object-fit: cover; border-radius: 4px;">
                    <% } else { %>
                      N/A
                    <% } %>
                  </td>
                  <td><%= event.title %></td>
                  <td><%= new Date(event.date).toLocaleDateString('en-IN', { dateStyle: 'medium' }) %></td>
                  <td><%= event.time || '-' %></td>
                  <td><%= event.location %></td>
                  <td><%= event.category?.name || '-' %></td>
                  <td><%= event.subcategory || '-' %></td>
                  <td><%= event.organizer || '-' %></td>
                  <td>
                    <% if (event.tiers && event.tiers.length) { %>
                      <ul class="list-unstyled mb-0">
                        <% event.tiers.forEach(tier => { %>
                          <li>
                            <strong><%= tier.name %>:</strong> ₹<%= tier.price %> (Qty: <%= tier.quantityAvailable %>)
                            <% if (tier.benefits && tier.benefits.length) { %>
                              <br><small>Benefits: <%= tier.benefits.join(', ') %></small>
                            <% } %>
                          </li>
                        <% }) %>
                      </ul>
                    <% } else { %>
                      No Tiers
                    <% } %>
                  </td>
                  <td><%=event.approvalStatus%></td>
                  <td><%= event.status %></td>
                  <td>
                    <a href="/organizer/events/edit/<%= event._id %>" class="btn btn-sm btn-warning mb-1">
                      <i class="fas fa-edit"></i> Edit
                    </a>
                    <form action="/organizer/events/delete/<%= event._id %>?_method=DELETE" method="POST" style="display:inline;">
                      <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure you want to delete this event?');">
                        <i class="fas fa-trash"></i> Delete
                      </button>
                    </form>
                    <% if (event.status === 'active') { %>
                      <form action="/organizer/events/deactivate/<%= event._id %>" method="POST" style="display:inline;">
                        <button type="submit" class="btn btn-sm btn-secondary mt-1">Deactivate</button>
                      </form>
                    <% } else { %>
                      <form action="/organizer/events/activate/<%= event._id %>" method="POST" style="display:inline;">
                        <button type="submit" class="btn btn-sm btn-success mt-1">Activate</button>
                      </form>
                    <% } %>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>

        </div>

        <!-- Footer -->
        <%- include('partials/footer') %>

      </div>
    </div>

  </div>

  <a class="scroll-to-top rounded" href="#page-top"><i class="fas fa-angle-up"></i></a>

  <!-- Scripts -->
  <script src="/vendor/jquery/jquery.min.js"></script>
  <script src="/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
  <script src="/vendor/datatables/jquery.dataTables.min.js"></script>
  <script src="/vendor/datatables/dataTables.bootstrap4.min.js"></script>
  <script src="/js/sb-admin.min.js"></script>

  <script>
    $(document).ready(function() {
      $('#eventsTable').DataTable();
      setTimeout(() => {
        $('.alert').fadeOut('slow');
      }, 3000);
    });
  </script>

</body>

</html>
